import express from "express";
// import { shop } from "#controllers";

const shopRouter = express.Router();

// shopRouter.post("/order/create", shop.createOrder);
// shopRouter.post("/order/place", shop.placeOrder);

export default shopRouter;
